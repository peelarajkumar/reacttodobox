import React from 'react'

export const RemoteChild = (props) => {
  return (

    <div>
        <center>
<button onClick={props.changeIt}>Remote Child Button</button>
        </center>
        </div>
  )
}
